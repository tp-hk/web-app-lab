import React, { Component } from 'react';  // needed for returning JSX in return <input />

// // define class component as follow
class SearchBar extends Component {
  // for class component, props is accessible anywhere using this.props

  render() {
    return <div>

    </div>
  }
}

// export a variable defined in this file
export default ComponentName;